services:
  - type: web
    name: ai-evolution-dashboard
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npx serve dist
    envVars:
      - key: NODE_VERSION
        value: 18